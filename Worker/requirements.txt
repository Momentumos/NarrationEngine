# Core async HTTP client for API communications
aiohttp>=3.8.0

# Environment variable management
python-dotenv>=0.19.0

# AWS SDK for S3 operations
boto3>=1.26.0
botocore>=1.29.0

# Audio processing (built-in wave module is used, no additional requirements)
# Audio conversion for MP3 format
pydub>=0.25.1

# For testing audio conversion (optional)
numpy>=1.21.0

# Standard library modules used: asyncio, json, os, time, uuid, datetime, typing, dataclasses, logging, concurrent.futures, threading, argparse, sys

# Optional: For better logging and debugging
colorlog>=6.7.0

# Optional: For enhanced error handling and monitoring
sentry-sdk>=1.15.0
